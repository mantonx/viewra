// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: scan_checkpoints.sql

package sqlc_postgres

import (
	"context"
	"database/sql"
)

const countFailedScanCheckpoints = `-- name: CountFailedScanCheckpoints :one
SELECT COUNT(*) FROM scan_checkpoints
WHERE scan_job_id = $1 AND status = 'failed'
`

func (q *Queries) CountFailedScanCheckpoints(ctx context.Context, scanJobID int64) (int64, error) {
	row := q.db.QueryRowContext(ctx, countFailedScanCheckpoints, scanJobID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createScanCheckpoint = `-- name: CreateScanCheckpoint :one
INSERT INTO scan_checkpoints (
    scan_job_id,
    file_path,
    status,
    file_size,
    file_hash,
    created_at
) VALUES ($1, $2, $3, $4, $5, CURRENT_TIMESTAMP)
RETURNING id, scan_job_id, file_path, status, file_size, file_hash, error_message, error_category, processed_at, created_at, retry_count
`

type CreateScanCheckpointParams struct {
	ScanJobID int64          `json:"scan_job_id"`
	FilePath  string         `json:"file_path"`
	Status    string         `json:"status"`
	FileSize  sql.NullInt64  `json:"file_size"`
	FileHash  sql.NullString `json:"file_hash"`
}

func (q *Queries) CreateScanCheckpoint(ctx context.Context, arg CreateScanCheckpointParams) (ScanCheckpoint, error) {
	row := q.db.QueryRowContext(ctx, createScanCheckpoint,
		arg.ScanJobID,
		arg.FilePath,
		arg.Status,
		arg.FileSize,
		arg.FileHash,
	)
	var i ScanCheckpoint
	err := row.Scan(
		&i.ID,
		&i.ScanJobID,
		&i.FilePath,
		&i.Status,
		&i.FileSize,
		&i.FileHash,
		&i.ErrorMessage,
		&i.ErrorCategory,
		&i.ProcessedAt,
		&i.CreatedAt,
		&i.RetryCount,
	)
	return i, err
}

const createScanCheckpointBatch = `-- name: CreateScanCheckpointBatch :exec
INSERT INTO scan_checkpoints (
    scan_job_id,
    file_path,
    status,
    file_size,
    file_hash,
    created_at
) VALUES ($1, $2, $3, $4, $5, CURRENT_TIMESTAMP)
`

type CreateScanCheckpointBatchParams struct {
	ScanJobID int64          `json:"scan_job_id"`
	FilePath  string         `json:"file_path"`
	Status    string         `json:"status"`
	FileSize  sql.NullInt64  `json:"file_size"`
	FileHash  sql.NullString `json:"file_hash"`
}

func (q *Queries) CreateScanCheckpointBatch(ctx context.Context, arg CreateScanCheckpointBatchParams) error {
	_, err := q.db.ExecContext(ctx, createScanCheckpointBatch,
		arg.ScanJobID,
		arg.FilePath,
		arg.Status,
		arg.FileSize,
		arg.FileHash,
	)
	return err
}

const deleteScanCheckpointsByJobID = `-- name: DeleteScanCheckpointsByJobID :exec
DELETE FROM scan_checkpoints
WHERE scan_job_id = $1
`

func (q *Queries) DeleteScanCheckpointsByJobID(ctx context.Context, scanJobID int64) error {
	_, err := q.db.ExecContext(ctx, deleteScanCheckpointsByJobID, scanJobID)
	return err
}

const getPendingScanCheckpoints = `-- name: GetPendingScanCheckpoints :many
SELECT id, scan_job_id, file_path, status, file_size, file_hash, error_message, error_category, processed_at, created_at, retry_count FROM scan_checkpoints
WHERE scan_job_id = $1 AND status = 'pending'
ORDER BY id ASC
LIMIT $2
`

type GetPendingScanCheckpointsParams struct {
	ScanJobID int64 `json:"scan_job_id"`
	Limit     int32 `json:"limit"`
}

func (q *Queries) GetPendingScanCheckpoints(ctx context.Context, arg GetPendingScanCheckpointsParams) ([]ScanCheckpoint, error) {
	rows, err := q.db.QueryContext(ctx, getPendingScanCheckpoints, arg.ScanJobID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ScanCheckpoint{}
	for rows.Next() {
		var i ScanCheckpoint
		if err := rows.Scan(
			&i.ID,
			&i.ScanJobID,
			&i.FilePath,
			&i.Status,
			&i.FileSize,
			&i.FileHash,
			&i.ErrorMessage,
			&i.ErrorCategory,
			&i.ProcessedAt,
			&i.CreatedAt,
			&i.RetryCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getScanCheckpointByID = `-- name: GetScanCheckpointByID :one
SELECT id, scan_job_id, file_path, status, file_size, file_hash, error_message, error_category, processed_at, created_at, retry_count FROM scan_checkpoints
WHERE id = $1
`

func (q *Queries) GetScanCheckpointByID(ctx context.Context, id int64) (ScanCheckpoint, error) {
	row := q.db.QueryRowContext(ctx, getScanCheckpointByID, id)
	var i ScanCheckpoint
	err := row.Scan(
		&i.ID,
		&i.ScanJobID,
		&i.FilePath,
		&i.Status,
		&i.FileSize,
		&i.FileHash,
		&i.ErrorMessage,
		&i.ErrorCategory,
		&i.ProcessedAt,
		&i.CreatedAt,
		&i.RetryCount,
	)
	return i, err
}

const getScanCheckpointByPath = `-- name: GetScanCheckpointByPath :one
SELECT id, scan_job_id, file_path, status, file_size, file_hash, error_message, error_category, processed_at, created_at, retry_count FROM scan_checkpoints
WHERE scan_job_id = $1 AND file_path = $2
`

type GetScanCheckpointByPathParams struct {
	ScanJobID int64  `json:"scan_job_id"`
	FilePath  string `json:"file_path"`
}

func (q *Queries) GetScanCheckpointByPath(ctx context.Context, arg GetScanCheckpointByPathParams) (ScanCheckpoint, error) {
	row := q.db.QueryRowContext(ctx, getScanCheckpointByPath, arg.ScanJobID, arg.FilePath)
	var i ScanCheckpoint
	err := row.Scan(
		&i.ID,
		&i.ScanJobID,
		&i.FilePath,
		&i.Status,
		&i.FileSize,
		&i.FileHash,
		&i.ErrorMessage,
		&i.ErrorCategory,
		&i.ProcessedAt,
		&i.CreatedAt,
		&i.RetryCount,
	)
	return i, err
}

const getScanCheckpointErrorsByCategory = `-- name: GetScanCheckpointErrorsByCategory :many
SELECT
    error_category,
    COUNT(*) as error_count
FROM scan_checkpoints
WHERE scan_job_id = $1 AND status IN ('failed', 'warning') AND error_category IS NOT NULL
GROUP BY error_category
`

type GetScanCheckpointErrorsByCategoryRow struct {
	ErrorCategory sql.NullString `json:"error_category"`
	ErrorCount    int64          `json:"error_count"`
}

func (q *Queries) GetScanCheckpointErrorsByCategory(ctx context.Context, scanJobID int64) ([]GetScanCheckpointErrorsByCategoryRow, error) {
	rows, err := q.db.QueryContext(ctx, getScanCheckpointErrorsByCategory, scanJobID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetScanCheckpointErrorsByCategoryRow{}
	for rows.Next() {
		var i GetScanCheckpointErrorsByCategoryRow
		if err := rows.Scan(&i.ErrorCategory, &i.ErrorCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getScanCheckpointProgress = `-- name: GetScanCheckpointProgress :one
SELECT
    COUNT(*) as total,
    SUM(CASE WHEN status IN ('completed', 'failed', 'warning') THEN 1 ELSE 0 END) as processed
FROM scan_checkpoints
WHERE scan_job_id = $1
`

type GetScanCheckpointProgressRow struct {
	Total     int64 `json:"total"`
	Processed int64 `json:"processed"`
}

func (q *Queries) GetScanCheckpointProgress(ctx context.Context, scanJobID int64) (GetScanCheckpointProgressRow, error) {
	row := q.db.QueryRowContext(ctx, getScanCheckpointProgress, scanJobID)
	var i GetScanCheckpointProgressRow
	err := row.Scan(&i.Total, &i.Processed)
	return i, err
}

const getScanCheckpointStats = `-- name: GetScanCheckpointStats :one
SELECT
    COUNT(*) as total_files,
    SUM(CASE WHEN status = 'pending' THEN 1 ELSE 0 END) as pending_files,
    SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) as completed_files,
    SUM(CASE WHEN status = 'failed' THEN 1 ELSE 0 END) as failed_files,
    SUM(CASE WHEN status = 'warning' THEN 1 ELSE 0 END) as warning_files,
    SUM(CASE WHEN status IN ('completed', 'failed', 'warning') THEN 1 ELSE 0 END) as processed_files,
    MIN(CASE WHEN status IN ('completed', 'failed', 'warning') THEN processed_at END) as first_processed_at
FROM scan_checkpoints
WHERE scan_job_id = $1
`

type GetScanCheckpointStatsRow struct {
	TotalFiles       int64       `json:"total_files"`
	PendingFiles     int64       `json:"pending_files"`
	CompletedFiles   int64       `json:"completed_files"`
	FailedFiles      int64       `json:"failed_files"`
	WarningFiles     int64       `json:"warning_files"`
	ProcessedFiles   int64       `json:"processed_files"`
	FirstProcessedAt interface{} `json:"first_processed_at"`
}

func (q *Queries) GetScanCheckpointStats(ctx context.Context, scanJobID int64) (GetScanCheckpointStatsRow, error) {
	row := q.db.QueryRowContext(ctx, getScanCheckpointStats, scanJobID)
	var i GetScanCheckpointStatsRow
	err := row.Scan(
		&i.TotalFiles,
		&i.PendingFiles,
		&i.CompletedFiles,
		&i.FailedFiles,
		&i.WarningFiles,
		&i.ProcessedFiles,
		&i.FirstProcessedAt,
	)
	return i, err
}

const listFailedScanCheckpoints = `-- name: ListFailedScanCheckpoints :many
SELECT id, scan_job_id, file_path, status, file_size, file_hash, error_message, error_category, processed_at, created_at, retry_count FROM scan_checkpoints
WHERE scan_job_id = $1 AND status IN ('failed', 'warning')
ORDER BY
    CASE
        WHEN status = 'failed' THEN 1
        WHEN status = 'warning' THEN 2
    END,
    processed_at DESC
LIMIT $2
`

type ListFailedScanCheckpointsParams struct {
	ScanJobID int64 `json:"scan_job_id"`
	Limit     int32 `json:"limit"`
}

func (q *Queries) ListFailedScanCheckpoints(ctx context.Context, arg ListFailedScanCheckpointsParams) ([]ScanCheckpoint, error) {
	rows, err := q.db.QueryContext(ctx, listFailedScanCheckpoints, arg.ScanJobID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ScanCheckpoint{}
	for rows.Next() {
		var i ScanCheckpoint
		if err := rows.Scan(
			&i.ID,
			&i.ScanJobID,
			&i.FilePath,
			&i.Status,
			&i.FileSize,
			&i.FileHash,
			&i.ErrorMessage,
			&i.ErrorCategory,
			&i.ProcessedAt,
			&i.CreatedAt,
			&i.RetryCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const resetFailedScanCheckpoints = `-- name: ResetFailedScanCheckpoints :exec
UPDATE scan_checkpoints
SET
    status = 'pending',
    error_message = NULL,
    error_category = NULL,
    processed_at = NULL
WHERE scan_job_id = $1 AND status = 'failed'
`

func (q *Queries) ResetFailedScanCheckpoints(ctx context.Context, scanJobID int64) error {
	_, err := q.db.ExecContext(ctx, resetFailedScanCheckpoints, scanJobID)
	return err
}

const updateScanCheckpointRetryCount = `-- name: UpdateScanCheckpointRetryCount :exec
UPDATE scan_checkpoints
SET retry_count = $1
WHERE id = $2
`

type UpdateScanCheckpointRetryCountParams struct {
	RetryCount int64 `json:"retry_count"`
	ID         int64 `json:"id"`
}

func (q *Queries) UpdateScanCheckpointRetryCount(ctx context.Context, arg UpdateScanCheckpointRetryCountParams) error {
	_, err := q.db.ExecContext(ctx, updateScanCheckpointRetryCount, arg.RetryCount, arg.ID)
	return err
}

const updateScanCheckpointStatus = `-- name: UpdateScanCheckpointStatus :exec
UPDATE scan_checkpoints
SET
    status = $1,
    error_message = $2,
    error_category = $3,
    processed_at = $4
WHERE id = $5
`

type UpdateScanCheckpointStatusParams struct {
	Status        string         `json:"status"`
	ErrorMessage  sql.NullString `json:"error_message"`
	ErrorCategory sql.NullString `json:"error_category"`
	ProcessedAt   sql.NullTime   `json:"processed_at"`
	ID            int64          `json:"id"`
}

func (q *Queries) UpdateScanCheckpointStatus(ctx context.Context, arg UpdateScanCheckpointStatusParams) error {
	_, err := q.db.ExecContext(ctx, updateScanCheckpointStatus,
		arg.Status,
		arg.ErrorMessage,
		arg.ErrorCategory,
		arg.ProcessedAt,
		arg.ID,
	)
	return err
}
