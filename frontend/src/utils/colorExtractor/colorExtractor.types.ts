/**
 * Type definitions for color extraction utilities
 */

/**
 * Color palette extracted from an image
 */
export interface ColorPalette {
  primary: string;
  secondary: string;
  accent: string;
}