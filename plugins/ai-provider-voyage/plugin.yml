# Voyage AI Provider Plugin Manifest
id: ai-provider-voyage
name: Voyage AI
version: 1.0.0
description: Embedding provider using Voyage AI - Anthropic's recommended embedding service

author: ViewRA Team
license: MIT
homepage: https://github.com/mantonx/viewra

min_host_version: "0.1.0"

# UI grouping (required) - valid values: search, recommendations, enrichers, providers, local, other
display_category: providers

# What this plugin provides (required)
capabilities:
  - provider
  - provider:voyage
  - embedding

# Required permissions
permissions:
  - network
